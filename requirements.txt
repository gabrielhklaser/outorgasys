# outorgasys - plataforma de automatizacao de processos de outorga (SIOUT RS)
# Testado em Python 3.11.
#
#   python -m venv .venv && .venv/bin/pip install -r requirements.txt
#   .venv/bin/python -m streamlit run app.py
#
# Stack geoespacial (Agente 2) e o coracao pesado da instalacao; em distribuicoes
# que usam PEP 668 use sempre um venv proprio.

# --- web / orquestrador ---------------------------------------------------------------
streamlit>=1.30
streamlit-folium>=0.20

# --- geoespacial (Agente 2) -----------------------------------------------------------
geopandas>=0.14
shapely>=2.0
pyproj>=3.6
pyogrio>=0.7
fiona>=1.9
rasterio>=1.3
folium>=0.15
contextily>=1.4
mapclassify>=2.6
xyzservices>=2023.10
geopy>=2.4

# --- calculo numerico / hidrogeologia (Agente 3) --------------------------------------
numpy>=1.26
pandas>=2.1
scipy>=1.11
openpyxl>=3.1
matplotlib>=3.8

# --- relatorio (Agente 5) --------------------------------------------------------------
reportlab>=4.0
pypdf>=4.0

# --- infra -----------------------------------------------------------------------------
requests>=2.31
